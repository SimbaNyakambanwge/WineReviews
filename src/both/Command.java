package both;
/**
 * Enum class for determine operation needed
 * @author Simbarashe Nyakambangwe
 * SID: 8316064
 */
public enum Command {
    SELECT,
    ADD,
    REMOVE,
    EDIT,
    FILTER,
    JOIN
}
